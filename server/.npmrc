# Ensure npm always resolves packages from the public registry.
# This prevents Replit's internal package-firewall URLs from leaking
# into package-lock.json and breaking Railway / EAS builds.
registry=https://registry.npmjs.org/
